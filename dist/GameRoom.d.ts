import { Server } from 'socket.io';
import { Card, CardType, GameState } from './types.js';
export declare class GameRoom {
    state: GameState;
    private io;
    private deck;
    private nopeTimeout;
    private turnTimeout;
    private onEmpty;
    private emptyTimeout;
    private readonly CLEANUP_DELAY;
    constructor(roomCode: string, io: Server, onEmpty: () => void);
    private addToLog;
    addPlayer(id: string, name: string): void;
    handleDisconnect(playerId: string): void;
    private checkEmptiness;
    clearEmptyTimeout(): void;
    kickPlayer(playerId: string): void;
    startGame(hostId: string): void;
    private nextTurn;
    private eliminatePlayer;
    private checkGameOver;
    private removeCardsFromHand;
    playCard(playerId: string, cardId: string, targetPlayerId?: string): void;
    playCombo(playerId: string, cardIds: string[], targetPlayerId: string, namedCard?: CardType): void;
    playNope(playerId: string, pendingActionId: string, cardId: string): void;
    private queueAction;
    private resetNopeTimer;
    private resolvePendingAction;
    private executeAction;
    drawCard(playerId: string): void;
    giveFavor(playerId: string, cardId: string): void;
    private handleKaboom;
    insertDefusedKaboom(playerId: string, index: number, card: Card): void;
    private getSanitizedPendingAction;
    private getSanitizedStateForPlayer;
    broadcastState(): void;
    private resetTurnTimer;
    private clearTurnTimer;
}
